FROM nginx:alpine

# 기본 nginx 설정 삭제
RUN rm /etc/nginx/conf.d/default.conf

# 커스텀 nginx 설정 복사 (로컬 개발용 - HTTP only)
COPY nginx/nginx.conf.dev /etc/nginx/conf.d/default.conf

# 포트 노출
EXPOSE 80

# nginx 실행
CMD ["nginx", "-g", "daemon off;"]
